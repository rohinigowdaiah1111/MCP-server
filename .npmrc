# Force devDependencies (typescript, @types/node) to install even when
# NODE_ENV=production is set — npm otherwise defaults to --omit=dev in that
# case, which breaks `npm run build` (tsc) on platforms like Render that
# apply the service's runtime env vars during the build step too.
# See deployment-plan.md, Phase 1.
include=dev
